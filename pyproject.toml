[project]
name = "grpc-python-react"
version = "0.1.0"
description = "gRPC Python backend for the React gRPC-Web course project"
requires-python = ">=3.13"
dependencies = [
    "grpcio==1.84.0",
    "grpcio-health-checking==1.84.0",
    "grpcio-tools==1.84.0",
    "pyjwt>=2.10",
]

[dependency-groups]
dev = []

[tool.uv]
package = false
